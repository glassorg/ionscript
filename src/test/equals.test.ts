// import { strict as assert } from "assert"
// import DataArray from "../DataArray"
// import "../index"
// import equals from "../internal/equals"
// import * as symbols from "../symbols"

// let a = new DataArray<any>(1, "foo", false)
// assert(a[symbols.hashNumber] !== 0)
// assert(equals(a, a))
// let b = new DataArray<any>(1, "foo", false)
// assert.strictEqual(a[symbols.hashNumber], b[symbols.hashNumber])
// assert(equals(a, b))
