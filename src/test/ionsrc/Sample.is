# import "foo/bar/baz" a, b
# import foo.Foo
#     x
#     y

# let x = foo?.bar[12 + 5]?.baz?("foo", true, x)

let identity = (a, b) =>
    try
        return 1
    catch e
        console.error(e)
    finally
        return 2
