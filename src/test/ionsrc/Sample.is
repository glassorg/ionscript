# import "foo/bar/baz" a, b
# import foo.Foo
#     x
#     y

# let x = foo?.bar[12 + 5]?.baz?("foo", true, x)

let identity = (a, b) =>
    switch a
        1, 2, 3
            return 10
        2
            return 20
        3
            return 30
        default
            return 100

